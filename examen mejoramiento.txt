SELECT 
    egresados.id,
    egresados.nombre,
    egresados.telefono,
    egresados.correo,
    empresa_egresados.Es_Actual,
    empresa_egresados.Area_empresa
FROM egresados
JOIN empresa_egresados ON egresados.id = empresa_egresados.id;

SELECT * 
FROM egresados 
WHERE id = (
    SELECT id 
    FROM egresado 
    WHERE id = 5
);


SELECT
courses.id AS course_id,
courses.titulo,
courses.descripcion,
courses.precio,
categories.id AS category_id,
categories.nombre
FROM courses
INNER JOIN categories 
ON courses.category_id = categories.id;

SELECT * 
FROM courses
WHERE id = (
    SELECT id 
    FROM courses
    WHERE id = 4
);


Filtrar
SELECT * FROM courses WHERE precio > 15;
ordenar
SELECT nombre FROM courses ORDER BY titulo DESC;
contar registro
SELECT COUNT(*) FROM courses;


iner join con apodos 
SELECT
c.id AS course_id,
c.titulo,
c.descripcion,
c.precio,
c.instructor_id,
p.id as category_id,
p.id,
p.nombre
FROM courses c
JOIN categories p ON p.id = c.category_id;